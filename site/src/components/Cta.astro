---
interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---
<div class="cta">
  <h2>{title}</h2>
  {description && <p>{description}</p>}
  <div class="row">
    <slot />
  </div>
</div>
